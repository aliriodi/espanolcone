import React from 'react'
const plan = require('./planes.json')

export default function Planasync({}) {

    const plan2 = [];
    const numberPlans = Object.keys(plan).length;
    Object.keys(plan).map(planX => plan[planX].type.includes )
  return (
    <div>
        
        Planasync


    </div>
  )
}
